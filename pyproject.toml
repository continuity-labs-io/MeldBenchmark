[project]
name = "meld-benchmark"
version = "0.1.0"
description = "MELD benchmark"

[tool.ruff]
line-length = 100
target-version = "py311"

[tool.ruff.lint]
select = [
    "F",  # pyflakes
    "C4", # flake8-comprehensions
    "B",  # flake8-bugbear
]
ignore = ["F401"]

[tool.ruff.format]
quote-style = "double"
indent-style = "space"

[build-system]
requires = ["setuptools>=61.0.0", "wheel"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
where = ["."]
include = ["src*"]

[tool.pyright]
reportUnusedImport = false
